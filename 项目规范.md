```markdown

\# 项目规范：SearXNG 联网搜索工具（Home Assistant 自定义集成）



\## 目标

做一个 HACS 自定义集成，复用已有的 SearXNG，让 Home Assistant 里的大模型

（如 DeepSeek）在需要时能联网搜索，并把结果融入回答。



\## 实现方式

\- 遵循 Home Assistant 官方 LLM 工具机制：提供一个 `llm.py`，暴露

&#x20; `async\_get\_tools` 回调，把「搜索」做成一个 LLM 可调用的工具。

\- 工具内部调用 SearXNG 的 JSON 接口（`/search?q=...\&format=json`），

&#x20; 取前几条结果的标题、链接、摘要返回给模型。

\- 用户通过界面配置 SearXNG 地址、可选账号密码、返回条数等参数。



\## 交付物（HACS 自定义仓库结构）

```

.

├── hacs.json

├── README.md

└── custom\_components/

&#x20;   └── searxng\_llm/

&#x20;       ├── manifest.json

&#x20;       ├── \_\_init\_\_.py

&#x20;       ├── config\_flow.py

&#x20;       ├── const.py

&#x20;       ├── llm.py

&#x20;       ├── tool.py

&#x20;       └── strings.json

```



\## 关键约束

\- 必须可被 HACS 以「自定义仓库」方式安装。

\- 全程异步（用 aiohttp），不引入任何第三方依赖。

\- SearXNG 地址、认证、条数等全部走配置界面，不硬编码。

\- 工具失败时抛标准异常，给出友好中文错误，不让 HA 崩溃。

\- README 说明安装步骤和前提：SearXNG 需开启 `format=json` 输出。



\## 验收标准

1\. HACS 能添加并安装，界面能搜到并配置。

2\. 问一个实时问题，模型会自动调用搜索，回答包含搜索结果。

3\. SearXNG 不可用时，能给出可理解的中文错误提示，系统不崩溃。



```



\---



